Test plans for fireTorpedo function (GT4500 class) based on specification

1. Check whether SINGLE mode fires only one store
2. Check if primary is empty then fires from secondary
3. Check if firing is alternating
4. Check if primary fired twice when secondary is empty
5. Check if return false when both are empty